Info from prof:

; CONFIG1H config FOSC = INTIO7 
; Oscillator Selection bits (Internal oscillator block, CLKOUT function on OSC2) config PLLCFG = OFF 
; 4X PLL Enable (Oscillator used directly) config PRICLKEN = ON 
; Primary clock enable bit (Primary clock is always enabled) config FCMEN = OFF ; Fail-Safe Clock Monitor Enable bit (Fail-Safe Clock Monitor disabled) config IESO = OFF 
; Internal/External Oscillator Switchover bit (Oscillator Switchover mode disabled) ; CONFIG2L config PWRTEN = OFF 
; Power-up Timer Enable bit (Power up timer disabled) config BOREN = SBORDIS 
; Brown-out Reset Enable bits (Brown-out Reset enabled in hardware only (SBOREN is disabled)) config BORV = 190 
; Brown Out Reset Voltage bits (VBOR set to 1.90 V nominal)
